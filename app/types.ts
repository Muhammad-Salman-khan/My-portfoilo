export type Datatype = {
  id: string;
  title: string;
  image: string;
  url: string;
  date: string;
  description: string;
  category: string;
  featured: boolean;
};

export type Post = {
  id: string;
  title: string;
  excerpt: string;
  slug: string;
  date: string;
};
